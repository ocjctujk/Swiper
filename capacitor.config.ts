import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.slider.com',
  appName: 'sliding-project',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
